# Привязка приложения splify2 к домену splify2_app.
#
# ЗАГОТОВКА — не рабочая строка. Здесь должно стоять имя пакета приложения splify2 и его
# уровень доверия, когда приложение появится. Пример для привилегированного приложения на
# платформенной подписи (тогда seinfo=platform обеспечивается mac_permissions.xml):
#
#   user=_app isPrivApp=true name=com.der.splify2 domain=splify2_app type=app_data_file levelFrom=all
#
# Пока пакета нет, строка закомментирована: раскомментировать и подставить настоящее имя
# пакета обязан владелец вместе с решением о подписи. Без неё домен splify2_app объявлен, но
# ни одно приложение в него не попадает — и это правильнее, чем привязать к чужому пакету.
